/** * INFO: Anda meminta project Node.js & Express dengan Frontend HTML biasa (Vanilla JS).
 * File next.config.js ini dibuat sesuai instruksi Anda, namun tidak akan digunakan
 * kecuali Anda merombak frontend ini menjadi project Next.js React Framework murni di masa depan.
 * * @type {import('next').NextConfig}
 */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["api.tmtreader.com"],
  },
};

export default nextConfig;
